export enum TransactionStatus {
  Approved = "Approved",
  Submitted = "Submitted",
  Rejected = "Rejected",
}

export enum KnownUnits {
  OP = "OP",
  BTC = "BTC",
  AVAX = "AVAX",
  STR = "STR",
  SUI = "SUI",
}
